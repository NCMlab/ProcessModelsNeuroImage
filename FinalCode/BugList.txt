The use of the call data.X(:,:,i) does not work and causes an error in the program that processes voxel by voxel.

Nboot needs to be added to the data array and then code needs to be cleaned up which is looking for it.
